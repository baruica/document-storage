language: php

sudo: false

php:
    - '7.0'

before_install:
    - composer self-update

install:
    - composer update --prefer-lowest --prefer-stable --prefer-dist --no-interaction

script:
    - make tests
